{
  "name": "@whyframe/tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm test:dev && pnpm test:build",
    "test:dev": "PW_DISABLE_TS_ESM=true playwright test",
    "test:build": "PW_DISABLE_TS_ESM=true TEST_BUILD=true playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.32.1",
    "execa": "^6.1.0",
    "tree-kill": "^1.2.2"
  }
}
